# 安全策略

[English](SECURITY.md) | **简体中文**

## 支持的版本

| 版本 | 是否支持 |
|------|----------|
| 1.0.x | :white_check_mark: |
| < 1.0 | :x: |

## 报告漏洞

请**不要**为安全问题开公开 Issue。

请通过 GitHub 的
[私密安全公告](https://github.com/OWNER/exifreader/security/advisories/new)
表单报告,或发邮件至 **security@example.com**。

请尽量附上:

* 版本(`exifreader --version`)与平台/编译器,
* 命令行与输出(含退出码),
* 能触发问题的最小或已简化输入文件,
* 问题属于崩溃、卡死、越界读/写、信息泄露,还是输出注入。

请去除样本中的真实元数据:EXIF 往往带有 GPS 坐标、序列号与设备标识。

## 处理时限

| 阶段 | 目标 |
|------|------|
| 确认收到 | 3 个工作日内 |
| 初步评估与严重度定级 | 7 天内 |
| 高危问题的修复或缓解 | 30 天内 |
| 公开公告与致谢 | 修复发布后与你协商确定 |

除你要求匿名外,我们会在公告与变更日志中致谢报告者。

## 威胁模型

exifreader 解析的是**不受信任**的文件。恶意图片是预期输入而非边缘情况,因此安全要求
如下:

* **畸形输入下不得出现内存安全问题。** 对文件缓冲区的每次访问都做边界检查;无符号偏移
  运算采用饱和而非回绕;目录条目数量在使用前会与文件剩余长度校验;越界与负的载荷指针
  (包括负的 `SLONG`/`SRATIONAL` 缩略图偏移)一律拒绝,而不是回绕。
* **不得有无界的工作量。** 目录环会被检测;IFD 递归有上限(`kMaxIfdDepth`,`--max-depth`
  夹紧到 256);每个目录的条目数有上限;访问过的目录总数有预算。
* **不得有输出注入。** 标签值中的控制字符与非法 UTF-8 在 tree/tags/summary 模式下会被
  转义,在 JSON 中替换为 U+FFFD,因此精心构造的载荷无法伪造行或输出终端转义序列。
* **JSON 永远合法。** 非有限浮点数输出为字符串 `"nan"`、`"inf"`、`"-inf"`,绝不会
  是裸 token。

项目预期在 ASan 与 UBSan 下都能构建并通过套件(CI 配置两者都跑),并能承受对解析器的
覆盖率引导模糊测试。仅在 sanitizer 下复现的报告仍然算有效的安全报告。

不在范围内:仅由体量巨大的**格式合法**文件造成的拒绝服务;以及工具按设计原样输出的数据
(例如 `--extract` 写出的缩略图字节)中存在的问题。
